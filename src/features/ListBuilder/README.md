```js
// const validateSequence = (items = []) => {
//   return items.reduce((prev, curr) => {
//     const foundIndex = prop('sequence')(prev);
//     console.log({ foundIndex });
//     return prev;
//   }, 0);
// };

// console.log(validateSequence(items));
```
